# INBOX — 사람이 던지는 지시

> 여기 적힌 것을 매 바퀴 가장 먼저 처리한다. SPEC·STATUS 보다 우선한다.
> 끝낸 항목은 지우지 않는다. `DONE.md` 맨 위로 옮긴다. 지우면 왜 그렇게 됐는지가 사라진다.
>
> **이 문서의 "처리할 것" 개수가 곧 합격 기준(①)이다.** 0건이 되면 라운드가 끝난다.
> 비어 있으면 루프는 아무것도 만들지 않고 대기한다.
>
> 위에서부터 순서대로 처리한다. 뒤 항목이 앞 항목의 레이아웃에 얹히므로 순서를 바꾸지 않는다.
> 1군이 끝났다 — 우측 컬럼(동료 + 동료 상태), 상단 HUD 칩, 좌측 사이드바 골격.
> **2군이 전부 끝났다** — 소음·무게·퀘스트 블록과 지도 확대까지.
> 3군은 다섯 건(섹션 헤더, 장소 카드 설명·요구치, 모서리 배지, 수량·내구도 게이지,
> 빈 슬롯 플레이스홀더)이 끝났다. **남은 것은 1건이고 그 1건은 막혀 있다** — 아래 항목의
> ⛔ 표시와 STATUS `막힌 것` 을 먼저 읽을 것. 사람이 정하기 전에는 루프가 대기한다.
> 목표: `reference/ui-refs/main-screen-v2.png` (구버전 `reference/main-screen-target.jpg` 보다 우선)
> 현재: **`reference/main-screen-2026-09-07.png`** (화면 전체, 1배)
> 부분 확대: `reference/carried-gauge-2026-09-07.png` (휴대 행 게이지) ·
> `reference/emptyslot-carried-2026-09-07.png` (빈 칸 `+` 표식) ·
> `reference/loccard-badge-sub-2026-09-07.png` / `reference/loccard-badge-district-2026-09-07.png`
> (확대본이 따로 있는 이유 — 배지 9px · 게이지 글자 8px 라 1배로는 눈으로 판정할 수 없다)

## 처리할 것

### 3군 — 섹션·카드 정보

> **사이드바에 남은 세로 여백은 13px 다** (`tools/capture-sidebar.mjs` 의 `freeBelow`).
> 3군은 보드 쪽 작업이라 사이드바를 건드릴 일이 없지만, 건드리게 되면 어디서 얼마를
> 가져왔는지는 새 결정이라 STATUS `결정과 근거` 에 남겨야 한다. 남기지 않으면 다음 바퀴가
> 되돌린다. 커밋 전 `scrolls: false` · `clipped` 없음을 매번 확인할 것.

- [ ] [2026-09-06] **장착 중 아이템을 목표처럼 강조한다.** — ⛔ **막혔다. 사람이 정해야 한다.**
  목표는 앰버 테두리 + `(Equipped)` 라벨. 현재는 표시가 약하다.
  `--accent-primary` 를 쓴다. 새 색을 만들지 않는다.
  검증: 장착/미장착 카드가 캡처에서 한눈에 구분된다.
  → [2026-09-07] **강조할 카드가 화면에 없다.** `EquipmentSystem.equip()` 이 장착하면서
  보드에서 그 카드를 지운다 (`js/systems/EquipmentSystem.js:81-84`). "표시가 약한" 게 아니라
  아예 없다. 고치려면 장착품이 휴대 칸을 차지하도록 규칙을 바꿔야 한다 — 칸 수·무게가 함께
  움직이는 게임 규칙 변경이다. 선택지 (A)/(B)는 STATUS `막힌 것` 에 적었다.
  → [2026-09-07] 근거를 코드로 **재확인했다. 막힘은 유효하다.** 루프는 여기서 대기한다.
  **사람이 (A) 또는 (B) 를 이 줄 아래에 적으면 다음 바퀴가 이어받는다.**
  → [2026-09-07] **대기 3바퀴째.** 답이 없는 동안 루프는 장부만 한 칸씩 넘긴다.
  (A)/(B) 어느 쪽도 고르기 싫으면 **이 항목을 지우고 `NEXT-ROUND-ITEMS.md` 로 옮기는 것도
  답이다** — 그러면 처리할 것이 0건이 되어 라운드가 끝난다.
  → [2026-09-07] **대기 4바퀴째.** 작업 트리는 2바퀴 연속 깨끗했고 답은 여전히 없다.
  루프는 이 항목에 손대지 않는다. 세 선택지 (A)/(B)/(이관) 중 하나를 이 줄 아래에 적어야
  다음 바퀴가 움직인다.
  → [2026-09-07] **대기 5바퀴째.** 작업 트리 깨끗(3바퀴 연속), 답 없음.
  **이 항목은 이제 라운드 전체를 막고 있는 유일한 건이다** — 나머지 12건은 전부 닫혔다.
  가장 싸게 라운드를 닫는 답은 (이관)이다: 이 항목을 지우고 `NEXT-ROUND-ITEMS.md` 로 옮기면
  처리할 것이 0건이 되고, 장착 강조는 규칙 변경을 다룰 수 있는 라운드에서 (A)로 제대로 한다.
  → [2026-09-07] **대기 6바퀴째.** 작업 트리 깨끗(4바퀴 연속), 답 없음.
  **여섯 바퀴 동안 저장소에 늘어난 것은 이 대기 기록뿐이다** — 이 항목이 열려 있는 한
  다음 바퀴도 똑같은 줄 하나만 남긴다. 답을 적을 자리는 바로 이 줄 아래다:
  `(A)` 장착품이 휴대 칸을 차지하게 규칙을 바꾼다 / `(B)` 장비 창으로 옮긴다 /
  `(이관)` 이 항목을 지우고 `NEXT-ROUND-ITEMS.md` 로 옮겨 라운드를 닫는다.
  → [2026-09-07] **대기 7바퀴째.** 작업 트리 깨끗(5바퀴 연속), 답 없음.
  루프는 이 항목을 스스로 판정할 수 없다 — (A)는 칸 수·무게가 함께 움직이는 **게임 규칙 변경**
  이고, SPEC 3절이 그 판단을 루프에 맡기지 않았다. (이관) 역시 라운드를 닫는 결정이라 사람 몫이다.
  그래서 답이 오기 전까지 매 바퀴 남는 것은 이 줄 하나뿐이다. **답을 적을 자리는 이 줄 아래다.**
  → [2026-09-07] **대기 8바퀴째.** 작업 트리 깨끗(6바퀴 연속), 답 없음.
  **이 항목 하나가 라운드 전체를 여덟 바퀴 붙잡고 있다.** 12건은 닫혔고 남은 것은 이것뿐이다.
  루프가 스스로 (이관)을 실행하지 않는 이유도 장부에 적어 뒀다 — 이관은 INBOX 를 0건으로
  만들어 **라운드를 끝내는 결정**이라 루프가 자기 종료 조건을 스스로 만들 수는 없다.
  세 줄 중 하나를 이 줄 아래에 그대로 옮겨 적으면 다음 바퀴가 움직인다:
  `(A)` 장착품이 휴대 칸을 차지하게 규칙을 바꾼다 — 칸 수·무게·교체 흐름이 함께 움직인다 /
  `(B)` 장비 창(`EquipmentModal`)으로 옮긴다 — 다만 모달은 SPEC 3절이 이 라운드에서 뺐다 /
  `(이관)` 이 항목을 지우고 `NEXT-ROUND-ITEMS.md` 로 옮겨 라운드를 닫는다 — 가장 싸다.
  → [2026-09-07] **대기 9바퀴째.** 작업 트리 깨끗(7바퀴 연속), 답 없음.
  세 선택지는 위 줄에 그대로 있다. 아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 10바퀴째.** 작업 트리 깨끗(8바퀴 연속), 답 없음.
  **열 바퀴 동안 이 항목에 관해 저장소에 늘어난 것은 대기 기록 열 줄뿐이다.** 세 선택지는
  위에 그대로 있고 루프는 셋 중 어느 것도 스스로 고를 수 없다. 아래에 한 줄만 적어라.
  → [2026-09-07] **대기 11바퀴째.** 작업 트리 깨끗(9바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 12바퀴째.** 작업 트리 깨끗(10바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 13바퀴째.** 작업 트리 깨끗(11바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 14바퀴째.** 작업 트리 깨끗(12바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 15바퀴째.** 작업 트리 깨끗(13바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 16바퀴째.** 작업 트리 깨끗(14바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 17바퀴째.** 작업 트리 깨끗(15바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 18바퀴째.** 작업 트리 깨끗(16바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 19바퀴째.** 작업 트리 깨끗(17바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 20바퀴째.** 작업 트리 깨끗(18바퀴 연속), 답 없음.
  **스무 바퀴다.** 이 항목이 열린 뒤 저장소에 늘어난 것은 대기 기록 20줄이 전부다.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 21바퀴째.** 작업 트리 깨끗(19바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 22바퀴째.** 작업 트리 깨끗(20바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 23바퀴째.** 작업 트리 깨끗(21바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.
  → [2026-09-07] **대기 24바퀴째.** 작업 트리 깨끗(22바퀴 연속), 답 없음.
  세 선택지 `(A)` / `(B)` / `(이관)` 은 대기 8바퀴째 줄 아래에 그대로 있다.
  아래에 한 줄만 적으면 다음 바퀴가 움직인다.

---

끝난 항목은 `DONE.md` 로 내려간다. 이 파일에는 남은 것만 둔다.
