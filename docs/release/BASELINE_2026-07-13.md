# 출시 준비 기준선 — 2026-07-13

> Phase 1(기준선 확정) 실측 기록. 이후 작업이 무엇을 망가뜨렸는지 이 문서와 비교해 판단한다.
> 관련 게이트 조건: `docs/release/RELEASE_GATES.md`

## 커밋 기준

전투 개편 WIP 61개 파일을 아래 4개 커밋으로 분할했다. 분할 전 상태는 `wip-backup-2026-07-13` 브랜치에 보존.

| 커밋 | 해시 | 내용 |
|------|------|------|
| chore(repo) | `92b5277` | tmp/output/outputs gitignore 등록, 산출물 재생성 경로 문서화 |
| feat(combat) | `ca75097` | 랭크 전투 파이프라인 + focused 전투 UI (엔진·데이터·UI·관련 테스트) |
| feat(combat-assets) | `a7aa905` | 스프라이트 시트 23종 + 감사/정규화 파이프라인 |
| docs(combat) | `783d6ee` | 전투 시뮬레이터 + 분석 문서 + 마스터플랜 + .claude 설정 |

엔진과 UI를 한 커밋으로 병합한 이유: 갱신된 통합 테스트(CombatPhase1 등)가 신규 엔진과 신규
UI 렌더러를 동시에 전제하므로, 분리 시 중간 커밋이 테스트 red가 된다.
`ca75097` 시점의 유일한 red는 `CombatSpriteSheetAssets.test.js`로, 이는 개편 전 HEAD(`67bb270`)에서도
실패하던 stale 테스트이며 `a7aa905`에서 해소됐다.

## 검증 실측값

| 항목 | 명령 | 결과 |
|------|------|------|
| 데이터 검증 | `node js/data/validate.js` | Errors 0 / Warnings 219 / ALL CLEAR |
| 에디터 도움말 커버리지 | `node tools/editor/check-help-coverage.mjs` | 아이템 필드 201개 전부 등록, exit 0 |
| 단위·통합 테스트 | `npm.cmd test` | 82파일 935개 전체 통과, 네트워크 에러 로그 0줄 |
| 전투 E2E | `npm run test:e2e:combat` | 통과 (`combat-screen:ok`) |
| PC 빌드 | `npm run build:pc` | 성공 (exit 0), `dist/pc/CardSurvival-RuinedCity-PC.exe` |

### Warnings 219 분류

전량 단일 유형 — **stackConfig.js 미등록 아이템 219개** (`raw_meat`, `pebble` 등).
스택이 안 될 뿐 기능 동작에는 지장 없으므로 **비차단**으로 분류.
개별 등록 판단은 로드맵 "드랍 검증" 단계에서 수행한다.
(그 외 material dead-end 10건은 validate가 경고 카운트에 포함하지 않는 정보성 출력.)

### localhost:3000 테스트 노이즈 (해결됨)

- 원인: `js/ui/combat/combatUiAssets.js`의 모듈 로드 시 매니페스트 `fetch()`가
  vitest jsdom 기본 문서 URL(`http://localhost:3000/`)로 실제 소켓 연결을 시도.
  jsdom 환경 전투 통합 테스트 7개 파일에서 각 1회씩 발생 (전체 로그 35줄).
- 조치: `_loadSpriteManifest()`에 `process.env.VITEST` 가드 추가 (`ca75097`에 포함).
- 검증: 수정 후 전체 스위트 ECONNREFUSED 0줄.

## 전투 밸런스 기준선

- 측정 도구: `node tools/sim/combat/run.mjs --n 100`
- 리포트: `docs/analysis/COMBAT_SIM_REPORT.md` (셀당 100회, 무기 6종 × 조우 5종)
- 이 커밋 기준으로 2026-07-13T15:09Z 재실행해 리포트 갱신 (기존 07-02 리포트 대체)
- **측정 조건 (고정)**: 플레이어 단독(동료 없음), 무방어구, HP 100, 사기 50(normal),
  소음 0, 스킬 레벨 초기화, 무기 매 전투 새 인스턴스 — 즉 **하한선 기준**.
  방어구/동료 축은 미포함이므로 실플레이 승률은 이보다 높다.
- 알려진 이슈 (Phase "전투 안정화/몬스터 패턴"으로 이관):
  - DL3 무장 승률 100% — 목표(60~75%) 대비 과잉. 적 그룹 크기/HP는 조우 테이블 영역.
  - 약탈자 2인조 vs 권총 13%·칼 13% 이상치 — 원거리 이동 턴 소모 + defensive AI의 원거리 우선 타겟.
  - stuck 발생 셀 7곳, DL5에 집중 (맨손 10회·칼 9회·마체테 6회 등) — 리포트 내 `⚠ stuck N` 표기.

## Phase 2(전투 안정화) 결과 비교 — 2026-07-14

Phase 2 커밋(`8474b41`~`249c865`) 반영 후 n=1000 재측정.
측정 축이 확장됨: 소음 조우 축(정숙 0/소음 45) + 실플레이 조건 리포트
(`COMBAT_SIM_REPORT_PARTY.md` — 전술조끼+간호사 동료) 분리.

| 지표 | Phase 1 기준선 | Phase 2 결과 (n=1000) | 판정 |
|------|---------------|----------------------|------|
| stuck | DL5 7셀 (guard_exceeded) | **양 조건 84,000회 전투에서 0셀** | 달성 — 자기 턴 사망 승리 미결선 엔진 버그 수정 |
| DL1 승률 | 무장 100%/맨손 94% | 92~100% (정숙) | 목표 90%+ 유지 |
| DL3 승률 | 100% (정숙 셀만 측정) | **소음45: 칼 60·배트 71·마체테 71·권총 72%** (소총 92) | 목표 60~75% 부합 — "100% 과잉"은 정숙 셀만 잰 측정 문제였음 |
| DL3 평균 라운드 | 1.4~2.5 | 소음45: 4.1~5.6 | 목표 4~7 부합 |
| DL5 실플레이 | (측정 축 없음) | 소음45: 58~94% | 목표 40~60% 대비 잔여 과잉 — 몬스터 패턴 단계로 이관 |
| 약탈자 2인조 | 권총 13%·칼 13% | 권총 14%·칼 20% (하한선) | 교착 해소 후에도 유지 — 버그 아닌 고정 조우 난이도·무기 상성. 패턴 단계 검토 |

구조 수정 2건이 수치를 움직인 주역:
1. 아군 인접 랭크 스왑(`045e5cd`) — 동료 동행 시 원거리 무기 봉인 해소 (수정 전 실플레이 권총/소총 60/60 move_failed).
2. 조우 규모 파티 스케일링(`249c865`) — 실플레이 DL5 87~100% → 58~94%.

## UI 기준선

- 스크린샷: `docs/release/assets/combat-ui-baseline-2026-07-13.png` (1920×1080, focused 레이아웃)
- 관찰 이슈:
  1. 스킬 카드 푸터 텍스트("위치 변경" 등)가 카드 하단에서 클리핑됨 — Phase "UI/플로우 개선"에서 처리.
  2. combat-test.html 하네스의 우측 테스트 사이드바가 적 HP 패널을 가림 — 하네스 한정, 실게임 영향 없음.
- 모바일/태블릿 대응: 당장 출시 계획 없음 — PC 전용으로 확정 (2026-07-14).

## 빌드 기준선

- `npm run build:pc` → `dist/pc/CardSurvival-RuinedCity-PC.exe` (portable, 1.44GB)
- 실행 스모크 (CDP 원격 디버깅으로 검증):
  - 부팅 → `screen-main-menu` 정상 렌더 (새 게임/이어하기/엔딩 갤러리/설정 표시)
  - 새 게임 클릭 → `screen-slot-select` 전환 확인
- **용량 주의**: exe 1.44GB의 대부분이 `assets/images` 1.3GB (sublocations 322MB, food 141MB 등).
  임시 산출물 혼입은 아님(files 화이트리스트 확인). 이미지 최적화(WebP 전환 등)는 RC QA 단계 과제.
- 아이콘 미설정: `default Electron icon is used` — Steam 준비 시 build-resources에 아이콘 필요.
- 코드사인 미설정: `signing is skipped` — Steam 배포 자체에는 필수 아님.

## 세이브 호환

- 세이브 저장소: localStorage `CARD_SURVIVAL_SAVE_v1_slot0~5` (5=자동 저장, `js/persistence/SaveManager.js`)
- 이번 개편은 `js/core/GameState.js`·`js/persistence/SaveManager.js`를 변경하지 않음
  (`git diff wip-backup-2026-07-13..HEAD` — 두 파일 diff 없음). 세이브 스키마 무변경.
- `GameState.deserialize`는 combat/encounter 상태로 저장된 세이브를 'main'으로 강제 복원하므로
  전투 중 상태가 세이브를 통해 이월되지 않는다. 신규 전투 필드는 `Object.assign` 병합으로
  기본값 유지 → 구버전 세이브는 구조적으로 호환.
- 실기기 구버전 세이브 이어하기 수동 확인 완료 (사용자, 2026-07-14).

## i18n

- `combat.*` 로케일 키: ko 125개 / en 125개, 양방향 누락 0.
- 전투 스킬 81종 전부 `combat.skill.<id>` ko/en 등록 확인 (COMBAT_SKILLS 전수 대조 스크립트 실행).
- 토큰 용어 `combat.token.*` 11종 ko/en 등록.

## Phase 3 재미 트랙(F1~F6) 결과 — 2026-07-14

측정: n=1000 × 2조건, 84,000회 전투 stuck 0셀 유지.

| 지표 | Phase 2 확정 | Phase 3 F트랙 후 | 판정 |
|------|-------------|-----------------|------|
| DL3 소음45 (하한선) | 60~72% | 칼 62·배트 73·마체테 75·권총 73% (소총 93) | 목표 60~75% 부합 유지 |
| DL5 소음45 (하한선) | 23~80% | 28~50% (소총 86) | 목표 40~60%에 정렬 (칼 28은 티어 미스매치로 수용) |
| DL5 소음45 (실플레이) | 58~94% | 58~84% (평균 HP 손실 54~91) | 승률은 목표 상회하나 승리 비용(HP 손실 70+)이 극심 — "준비된 파티의 보상"으로 수용, 잔여는 난이도 옵션/조우 테이블 설계 소관으로 이관 |
| 거대 좀비(brute) | 칼 24% | 칼 58% (예고 대응 시) | slam 예고제 — 읽으면 살고 못 읽으면 아픈 구조 |
| 약탈자 2인조 | 권총 14·칼 20% | 권총 28·칼 33% | 조준 취소 카운터로 이상치 완화 |

주요 변경: 텔레그래프 7종(F1), 넉백/끌기+auto 이동(F2), 직업 6종 셋업→페이오프(F3),
약점 발견·표기(F4), 스트레스 동요·붕괴 잠금·상황식 도주(F5), 동료 공용 유틸 스킬(guard/reposition).
